[build-system]
requires = []
build-backend = "nap00_wheel_backend"

[project]
name = "plotpilot-provider-anthropic"
version = "0.1.0"
description = "PlotPilot Anthropic provider package"
requires-python = ">=3.12,<3.13"
