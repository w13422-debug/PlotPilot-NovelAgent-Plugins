[build-system]
requires = []
build-backend = "nap00_wheel_backend"

[project]
name = "plotpilot-source-structure"
version = "0.1.0"
description = "PlotPilot Novel-Agent source structure and evidence plugin"
requires-python = ">=3.12,<3.13"