[build-system]
requires = []
build-backend = "style_runtime.build_backend"
backend-path = ["."]

[project]
name = "plotpilot-style-runtime"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
description = "Exact qualified Style apply review and refine Code Plugin"
