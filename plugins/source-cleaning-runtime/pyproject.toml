[build-system]
requires = []
build-backend = "nap00_wheel_backend"

[project]
name = "plotpilot-source-cleaning-runtime"
version = "0.1.0"
description = "PlotPilot Novel-Agent deterministic source cleaning runtime"
requires-python = ">=3.12,<3.13"
